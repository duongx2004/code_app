name: code_app
description: "Hoc Code"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  flutter_highlight: ^0.7.0
  google_fonts: ^6.2.1
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  code_text_field: ^1.1.0
  highlight: ^0.7.0
  http: ^1.2.2
  cupertino_icons: ^1.0.8
  connectivity_plus: ^6.0.0
  dio: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/